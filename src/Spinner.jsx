import React from "react";
import "./Spinner.css";

const Spinner = () => {
  return (
    <div className="w-full h-full">
      <span className=".loader"></span>
    </div>
  );
};

export default Spinner;
